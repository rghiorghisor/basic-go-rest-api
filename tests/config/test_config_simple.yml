server:  
  http:
    port: 8081
    # Read timeout (in seconds).
    read-timeout: 10
    # Write timeout (in seconds).
    write-timeout: 11

storage:
  mongo:
    uri: "mongodb://localhost:27017"
    name: "testdb"   